---
import BaseLayout from "../layouts/BaseLayout.astro";
import BeatCard from "../components/beats/BeatCard.astro";
import { beats } from "../data/beats";
---

<BaseLayout title="Accueil | MokingJayy" showPlayer={true}>
  <!-- Hero Section -->
  <section class="relative min-h-screen flex items-center justify-center overflow-hidden">
    <!-- Image de fond -->
    <div class="absolute inset-0 z-0">
      <div class="absolute inset-0 bg-black/40"></div>
      <img 
        src="/images/_DSF7911.JPG" 
        alt="MokingJayy" 
        class="w-full h-full object-cover object-center"
      />
    </div>
    
    <div class="container mx-auto px-4 relative z-10 text-center">
      <h1 class="text-5xl md:text-8xl lg:text-9xl font-light tracking-tight text-white mb-6">
        <span class="block font-serif italic font-light mb-2">Je suis</span>
        <span class="block font-bold uppercase tracking-wider">MokingJayy</span>
      </h1>
      
      <div class="w-24 h-1 bg-white/50 mx-auto my-8"></div>
      
      <p class="text-xl text-white/90 mb-12 leading-relaxed max-w-2xl mx-auto">
        Producteur de musique passionné par la création de sons uniques et inspirants
      </p>
      
      <div class="flex flex-wrap justify-center gap-6">
        <a 
          href="#featured-beats" 
          class="px-8 py-4 bg-white text-gray-900 rounded-lg font-medium hover:bg-white/90 transition-all transform hover:-translate-y-1 shadow-lg hover:shadow-xl"
        >
          Découvrir mes beats
        </a>
        <a 
          href="/contact" 
          class="px-8 py-4 border-2 border-white text-white rounded-lg font-medium hover:bg-white/10 transition-all transform hover:-translate-y-1"
        >
          Me contacter
        </a>
      </div>
    </div>
    
    <!-- Flèche de défilement -->
    <a 
      href="#featured-beats" 
      class="absolute bottom-10 left-1/2 transform -translate-x-1/2 text-white animate-bounce z-20"
      aria-label="Défiler vers le bas"
    >
      <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </a>
  </section>

  <!-- Featured Beats -->
  <section id="featured-beats" class="py-24 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <span class="text-sm font-semibold tracking-wider text-gray-500 uppercase">Dernières créations</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-light text-gray-800">Beats populaires</h2>
        <div class="mt-4 w-20 h-0.5 bg-gray-300 mx-auto"></div>
      </div>
      
      <!-- Filtres -->
      <div class="flex flex-wrap justify-center gap-3 mb-16">
        <button class="px-5 py-2 bg-gray-800 text-white rounded-full text-sm font-medium hover:bg-gray-700 transition-all">
          Tous
        </button>
        {['Trap', 'Hip Hop', 'R&B', 'Drill', 'Afro', 'Pop'].map((genre) => (
          <button class="px-5 py-2 text-gray-500 rounded-full text-sm font-medium hover:bg-gray-200 transition-all">
            {genre}
          </button>
        ))}
      </div>

      <!-- Grille de beats -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {beats.map((beat) => (
          <BeatCard beat={beat} />
        ))}
      </div>

      <div class="text-center mt-10">
        <a 
          href="/beats" 
          class="inline-block bg-gray-800 text-white px-8 py-3 rounded-full font-medium hover:bg-gray-700 transition-all"
        >
          Voir tous les beats
        </a>
      </div>
    </div>
  </section>

  <!-- À propos -->
  <section class="py-24 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="md:flex items-center gap-16">
          <div class="md:w-1/2 mb-10 md:mb-0">
            <div class="relative">
              <div class="w-full h-80 bg-gray-50 rounded-2xl overflow-hidden border border-gray-100">
                <!-- Remplacez par votre image -->
                <div class="w-full h-full bg-gradient-to-br from-gray-50 to-gray-100 flex items-center justify-center">
                  <span class="text-gray-300 text-sm">Votre image ici</span>
                </div>
              </div>
              <div class="absolute -bottom-4 -right-4 w-24 h-24 bg-gray-800 rounded-full border-4 border-white flex items-center justify-center shadow">
                <span class="text-white text-4xl">🎵</span>
              </div>
            </div>
          </div>
          <div class="md:w-1/2">
            <span class="text-sm font-semibold tracking-wider text-gray-500 uppercase">À propos</span>
            <h2 class="mt-2 text-3xl font-light text-gray-800 mb-6">Qui suis-je ?</h2>
            <p class="text-gray-500 mb-8 leading-relaxed">
              Je suis MokingJayy, producteur passionné par la création d'univers sonores uniques. 
              Chaque beat est conçu avec soin pour offrir une expérience auditive distinctive et inspirante.
            </p>
            <a 
              href="/about" 
              class="inline-block border-2 border-gray-800 text-gray-800 px-8 py-3 rounded-lg font-medium hover:bg-gray-800 hover:text-white transition-colors"
            >
              En savoir plus
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="relative py-24 bg-gray-800 overflow-hidden">
    <div class="absolute inset-0 bg-white opacity-5"></div>
    <div class="container mx-auto px-4 text-center relative z-10">
      <h2 class="text-3xl md:text-4xl font-light text-white mb-6">Prêt à donner vie à votre musique ?</h2>
      <p class="text-xl text-white text-opacity-90 mb-10 max-w-2xl mx-auto leading-relaxed">
        Découvrez des sonorités uniques qui sublimeront votre prochain projet créatif.
      </p>
      <div class="space-y-4 sm:space-y-0 sm:space-x-4">
        <a 
          href="/beats" 
          class="inline-block bg-white text-gray-800 px-8 py-4 rounded-lg font-medium hover:bg-opacity-95 transition-all transform hover:-translate-y-1 shadow-sm hover:shadow"
        >
          Explorer les beats
        </a>
        <a 
          href="/contact" 
          class="inline-block border-2 border-white text-white px-8 py-4 rounded-lg font-medium hover:bg-white hover:bg-opacity-10 transition-all transform hover:-translate-y-1"
        >
          Me contacter
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
